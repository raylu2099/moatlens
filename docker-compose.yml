version: '3.8'

services:
  web:
    build: .
    ports:
      - "8502:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./.env:/app/.env:ro
      - ./demo:/app/demo:ro
      - ./docs:/app/docs:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
