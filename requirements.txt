# Moatlens — Python 3.12, runtime deps only.
# For dev/test tooling, see requirements-dev.txt.
# The lockfile requirements.lock.txt is the NAS micromamba ytdlp env
# snapshot (shared with yt-dlp) — not a Moatlens-specific pin.

# Core
pydantic>=2.6.0
python-dotenv>=1.0.0

# Data providers
requests>=2.32.0       # >= 2.32 closes CVE-2024-35195
yfinance>=0.2.40
anthropic>=0.40.0

# Numeric
numpy>=1.24.0

# Web framework (single-user, local only)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.9
markdown>=3.6
pyyaml>=6.0

# CLI UX
rich>=13.7.0
typer>=0.12.0
