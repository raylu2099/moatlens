# Dev / test tooling. Not installed in prod (Dockerfile uses requirements.txt).
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# Test runner
pytest>=7.0.0

# Linter + formatter (also runs via .pre-commit-config.yaml against ruff-pre-commit)
ruff>=0.6.9

# Git hook runner
pre-commit>=3.7.0
